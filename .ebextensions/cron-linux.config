files:
    "/etc/cron.d/mycron":
        mode: "000644"
        owner: root
        group: root
        content: |
            * * * * * root curl -X POST https://api.test.com/api1 >> /var/log/job1.log 2>&1
commands:
    remove_old_cron:
        command: "rm -f /etc/cron.d/mycron.bak"